# State

Updated: 2026-08-23

## Done in this build block

- Empty public repository initialised as a Vinext/React PWA.
- Product-specific employee sign-in, first-PIN replacement and work-area choice.
- Event Work tabs: Home, Updates, Malur, Taj and core-only Budget.
- Responsive Event activity list and assignment-aware activity detail flow.
- D1 schema and migrations for 25 tables covering identity, Event Work, Budget, sessions, sync/audit, Guest Coordination, messaging, attachment metadata and durable Google Sheets delivery.
- Protected authentication, session, PIN change, snapshot, job update, reassignment, Budget and Master import endpoints.
- Master contract for the exact `1 People`, `2 Sections` and `3 Jobs` headers.
- Install manifest, offline route, service worker and explicit update prompt.
- Guest Coordination tabs: Mine, Invitations, Guests, Travel and Stays.
- Unified Guest Master validation for Guest Categories, Guest Groups, Guests, Group Agenda, Travel Plans, Travel Stops and Hotels.
- Guest database structure for categories, groups, event-specific invitations/RSVP, dated agenda, category travel, individual stays, templates and message batches/results.
- Explicit English, German or Japanese preference per guest and 24 fixed draft templates covering four message purposes and two channels.
- Core-committee template review/approval, recipient-specific message preflight and actionable `Not sent` reasons.
- Guest add/edit/archive endpoints, coordinator-owned agenda editing, category travel editing and name-search hotel/room assignment.
- Public event-specific RSVP route with only `Yes, I’ll attend` and `Unable to attend` responses.
- Version-bound Master impact preview and explicit confirmation before any import can apply.
- Master/app provenance is preserved when editing existing guests, agenda lines, travel plans and travel stops; app-created guest rows remain protected from Master replacement.
- Unified 11-sheet test Master generated outside Git with dynamic row handling and explicit missing-data warnings.
- Parent-bound agenda-line and travel-stop mutations prevent client-supplied child IDs from moving records across guest groups or travel plans.
- Protected Event-update media: bounded phone file chooser, server MIME/signature validation, private R2 storage, D1 metadata, authenticated no-store reads and byte-range playback.
- App-managed Budget creation and Travel-plan creation, confirmed guest archive with Google Sheets write-back, and work-area switching from both app headers.
- Phone/browser Back and Forward history across work-area, Event tabs and Guest tabs; narrow-screen agenda/travel remove controls now meet the 44px target.
- Master apply responses now report the archive impact from the exact confirmed preview, including rows omitted from a replacement workbook.
- Google Sheets integration source: permanent-ID app-to-Sheet upserts, a coalescing D1 outbox with bounded retry, Apps Script locking, Sheet-to-app impact preview and exact-version apply confirmation.
- Release-specific service-worker generation and update checks on registration, focus, reconnection and visibility changes.
- A tracked, production-target-guarded staged load harness for 10, 25, 40, 75 and 100 authenticated sessions.

## Evidence so far

- Supplied test workbook structural check: 22 of 22 People, 13 of 13 Sections and 140 of 140 source Jobs were accepted. `Both` expansion produced 202 operational jobs; 0 validation issues or unresolved references. These figures are test evidence, not product limits.
- Automated build and test run: 10 of 10 passed, 0 failed.
- TypeScript compile and ESLint: both passed with 0 errors.
- Browser interaction checks: 5 of 5 Event Work tabs opened; the tested activity save changed Home progress and appeared in Updates.
- Responsive browser checks: login and Event Work had 0 horizontal-overflow failures at 320×568 and 360×800; login also had 0 vertical-overflow failures at both phone sizes. Desktop 1440×900 had 0 horizontal-overflow failures.
- Browser console check after the completed phone workflow: 0 errors and 0 warnings.
- Dependency audit: 0 vulnerabilities across 6 production dependencies. The complete development-tool audit reports 4 moderate transitive findings under `drizzle-kit`; npm offers no non-breaking fix.
- Complete automated run after Guest integration: 16 of 16 tests passed, 0 failed; the production build completed.
- ESLint after Guest integration: 0 errors and 0 warnings.
- Guest browser checks: 5 of 5 tabs opened at 360px and 320px. Horizontal-overflow failures were 0 of 5 at each width after the Mine correction.
- At 320px, visible controls below 44px were 0 in each of the 5 Guest tabs.
- Browser workflows reproduced: guest language selection/save, filtered invitation audience count, agenda-line save, category-travel save, guest-name hotel/room save, 6 invitation template variants and a send preflight with 1 ready and 1 not-sent recipient.
- Browser console after the completed Guest phone workflow: 0 errors and 0 warnings.
- Unified test Master verification: 11 sheets, 1,845 of 1,845 supplied test guest rows, 0 schema issues and 0 formula-error matches. Warnings were explicit for 130 missing finish-by dates, 74 missing emails, 252 missing mobile numbers and empty agenda/travel input.
- Synthetic local D1 system workflow: 10 of 10 HTTP checks passed across Master add, login, Event reflection, progress save, Guest reflection, app add/edit, removal impact preview, Master replacement and app archive.
- Latest isolated historical-preservation readback after archive: active jobs 0, active guests 0; 4 invitation rows, 1 stay row, 2 job-state rows, 1 job update, 9 audit rows and 2 applied sync batches remained.
- Unified test Master local import: preview completed in 910 ms; one atomic apply completed in 3,759 ms and read back 22 active people, 13 sections, 202 operational jobs, 1,845 guests and 3,690 invitation rows.
- Local 40-client realistic-fixture run: login 40 of 40 succeeded (p95 10,845 ms), Event snapshot 40 of 40 succeeded (p95 5,627 ms) and Guest snapshot 40 of 40 succeeded (p95 10,214 ms). This is single-process local evidence, not production D1 capacity evidence.
- Single local snapshot size: Event 71,692 raw bytes / 8,866 gzip bytes; Guest 1,067,498 raw bytes / 123,330 gzip bytes.
- Latest release verification: production build passed, ESLint passed, 16 of 16 automated tests passed and the production dependency audit found 0 vulnerabilities across 6 production dependencies.
- Security scan of 74 repository files reported 2 launch risks in the approved PIN model: one high shared-bootstrap-PIN risk and one medium account-enumeration/lockout risk. The owner-only preview contains both during private testing; public launch does not.
- Security diff review covered 10 of 10 changed runtime files and reproduced 1 medium cross-group agenda-ID authorization defect. Before the fix, the crafted request returned 200 and moved the other group's item; after the fix it returned 409, while a legitimate same-group update and a new line both returned 200. The sibling travel-stop test produced the same 409/200 protected/legitimate result.
- Latest phone audit measured 32 screen/viewport combinations across 320×568 and 360×800: 0 horizontal-overflow failures, 0 controls below 44px, 0 login vertical-overflow failures and 0 browser console errors/warnings. Numeric, decimal, phone, email, date/time and media inputs were focusable with the intended input modes.
- Latest synthetic local D1/R2 lifecycle passed 18 of 18 HTTP checks: Master add/reflection/removal, login, forged upload rejection, protected full/range media readback, invalid-range rejection, Budget validation, app/Master guest rules, Travel add/conflict, history retention and app-row protection.
- Latest working-tree security diff scan reviewed 21 of 21 authoritative runtime/configuration files and produced 0 reportable findings. TAC status was unverified because the advisory connector was not connected; the parent agent performed all 21 reviews because delegated workers were unavailable for this scan.
- Latest release run completed the production build and passed 21 of 21 automated tests; the production dependency audit still reports 0 vulnerabilities.
- Private Cloudflare-backed Sites version 4 deployed successfully. Its live D1 read-back exposes all 24 expected application tables, including attachment metadata.
- Remote unified test Master import first reproduced one Cloudflare PBKDF2 limit failure at 210,000 iterations. After changing the portable setting to Cloudflare's 100,000-iteration ceiling and rerunning the test/build, the second preview and apply succeeded: 2,106 applied, 0 rejected, 0 warnings and 0 archived. The live `sync_batches` row independently reads `applied` with the same 2,106/0 counts.
- The 2,106 remote test-Master records comprise 22 people, 13 sections, 202 operational jobs, 11 guest categories, 9 guest groups, 1,845 guests and 4 hotels. The remote attachment table is empty as expected before a user uploads media.
- Exhaustive phone interaction pass initially produced 189 passes from 190 measured checks and reproduced one real failure: browser Back left the PWA instead of moving through app screens. After the history fix, the six exact Back/Forward paths across Event and Guest work areas passed 6 of 6; the corrected 320px agenda and travel remove targets measured 44×48 and 44×46 respectively.
- Live private D1/R2 acceptance passed 49 of 49 checks across authentication/PIN replacement, Event snapshots and updates, forged-media rejection, private full/range photo reads, Budget, Guest create/edit/archive, templates and channel preflight, Stay, Agenda, Travel, sign-out and later login.
- Live replacement-Master removal/restore passed 10 of 10 checks: the preview reported one impacted Guest, confirmed apply removed it from active Guest workflows, restoration returned it, and the final preview reported 0 remaining archive impacts.
- Final regression completed the production build and passed 22 of 22 automated tests; ESLint reported 0 errors and 0 warnings, the production dependency audit reported 0 vulnerabilities, and `git diff --check` passed.
- Final security diff review closed all 4 changed runtime source files with 0 candidates and 0 reportable findings. TAC status remained unverified because its advisory connector was not connected; all four inventory files were reviewed by the parent agent.
- Private production staged load completed 1,000 of 1,000 HTTP operations with 0 failures: 250 logins, 500 authenticated Event/Guest snapshot reads and 250 logouts across 10/25/40/75/100-session stages. At 100 sessions, login p95 was 4,399 ms, the two snapshot-wave p95s were 7,154 ms and 7,015 ms, and logout p95 was 4,435 ms. This is capacity evidence for the current test fixture, not real-data accuracy evidence; the measured snapshot latency remains an optimisation target.
- Release-readiness build completed and all 25 of 25 automated tests passed; ESLint and `git diff --check` passed with 0 errors.
- A blind independent baseline audit reproduced 0 of 1 Google connectors, 0 of 1 update prompts with byte-identical service workers and no safe tracked production load harness. The subsequent implementation directly addresses all three reproduced gaps; live Google delivery remains unproven until real connector credentials are supplied.
- Private Sites release 6 installed the byte-distinct `yil-event-shell-aea173091a4c` worker over the previous fixed `v2` worker. The browser displayed 1 of 1 update prompts; selecting Refresh update produced 1 of 1 controlled reloads, removed the old cache and left 0 waiting workers.
- Private Sites release 7 then produced a second byte-distinct worker, `yil-event-shell-bc13e14cee94`. It reached `installed/waiting` in 1 of 1 checks, showed the update banner, and the user-facing refresh promoted it in 1 of 1 checks; the old release-6 cache was removed and 0 waiting workers remained.
- Independent re-audit passed the PWA transition and staged-load harness, then reproduced three Sheets edge cases plus one standalone TypeScript configuration failure. The follow-up closes them with row-hash conflict detection, post-apply Sheet baselines, automatic permanent IDs for human-created rows, background delivery/retry during app writes and active Guest reads, partial-batch acknowledgement, and an explicit no-emit TypeScript configuration. Final re-verification passed 25 of 25 tests, ESLint, standalone TypeScript and `git diff --check`.
- The verified 11-sheet test Master is now a native Google Sheet in the customer-owned Google account, and its deployed Apps Script `/exec` connector directly returned 22 people, 140 source jobs, 1,845 guests, 11 categories and 9 groups with HTTP 200.
- The first production Sheet preview reproduced HTTP 502 with `The operation was aborted`: the real 1,845-guest export took longer than the previous 8-second client timeout. Release 9 raised only that bounded connector timeout to 30 seconds; the production build, 25 of 25 tests, ESLint and standalone TypeScript all passed before deployment.
- Post-fix live two-way Sheet acceptance passed 19 of 19 checks. Exact-version pull/apply baselined 2,106 records with 0 archives; a disposable app guest was then created, updated and archived, all three states were independently read back from Google Sheets, the archived guest left active app workflows, and final outbox status was 0 pending / 0 failed.
- Provider-backed message delivery is implemented behind an explicit Send confirmation and bounded batches. Recipient contacts and rendered variables are frozen at preflight, each send is atomically claimed, RSVP links are created only at invitation send time, only token hashes are stored, test sends are server-allowlisted, and ambiguous provider responses are retained as `delivery-unknown` without blind retry.
- Release-candidate verification after the delivery implementation completed the production build and passed 28 of 28 automated tests. ESLint, standalone TypeScript, `git diff --check`, and the production dependency audit all passed; the audit found 0 known vulnerabilities across 6 production dependencies.
- The first delivery-version publish was stopped before release because Cloudflare D1 rejected SQLite's non-constant `ALTER TABLE` default. The replacement table-rebuild migration then executed 100% of six migration stages against a seeded legacy recipient and preserved 10 of 10 checked values plus all three expected indexes.
- Private Sites version 11 deployed the corrected delivery release with environment revision 4. Live D1 read-back exposed all 13 expected `message_recipients` columns after migration.
- The first allowlisted live email acceptance completed core login, delivery configuration, Guest snapshot, fixed-template approval, dummy Guest creation, preflight, provider send, post-send snapshot and state assertions. Exactly 1 message was provider-accepted with a provider ID; the batch completed with 0 failed and 0 delivery-unknown. Inbox arrival, rendering and RSVP click remain separate human acceptance checks.
- A dual-channel invitation test reproduced one Cloudflare edge incompatibility before the WhatsApp request could complete: the runtime rejected `redirect: "error"`. Release 13 changed both Acele paths to supported manual redirect handling, refuses 3xx responses without following them, and retains ambiguous outcomes without blind retry. The focused regression passed 1 of 1, the release module passed 6 of 6, and the full run passed 31 of 31 tests plus build, lint, TypeScript, diff check and a 0-vulnerability production audit.
- The resumed allowlisted live messaging acceptance then provider-accepted 7 of 7 previously unattempted messages: 3 WhatsApp and 4 email across invitation, agenda, travel and stay. Independent D1 readback found 7 batches, 7 recipients, 7 provider IDs and 7 single attempts, with 7 accepted, 0 failed and 0 delivery-unknown. Two RSVP token hashes remained for the same guest/event, proving one channel did not overwrite the other channel's link state. Phone/inbox arrival, wording inspection and RSVP clicks remain human checks.
- Release 14 added the missing Travel and Stay send actions to the Guest UI. Travel sends are now bound from the selected route card through the send sheet to an exact travel-plan ID at server preflight, preventing another day or route for the same category/event from being selected. The focused UI-chain regression passed 1 of 1, the rendered-boundary module passed 7 of 7, the surrounding Guest suite passed 21 of 21, and the final run passed 32 of 32 tests plus build, lint, TypeScript, diff check and a 0-vulnerability production audit.
- Private Sites version 14 deployed successfully from exact commit `4c87c3d8af1a09dab95dcf27b0c66139da8976b8` with environment revision 4.
- Live version-14 Travel preflight rejected 1 of 1 requests that omitted the route ID with `Choose the exact travel plan to send`, then accepted 1 of 1 otherwise-identical requests carrying the exact plan ID with 1 ready and 0 skipped. No external message was sent by this check.
- The first physical RSVP-link open reproduced the Sites owner-only ChatGPT sign-in gate before the application route. After explicit owner approval, Sites access revision 2 changed to public for dummy-data RSVP testing. An unauthenticated reproduction returned HTTP 200 at the YIL URL with the employee login present and no ChatGPT gate. Unauthenticated Event, Guest and Budget requests were denied 3 of 3 with HTTP 401. Production approval of the shared bootstrap PIN remains open.
- Physical RSVP acceptance is now proven on the authorised phone: the live D1 audit recorded `pending` to `accepted` at `2026-08-21T12:05:23.774Z`, and an independent authenticated Guest snapshot read the same Malur invitation as `accepted`. This verifies the public link, token lookup, database update and employee-side readback for the QA record.
- A fresh production Master preview reproduced 2 of 2 valid Google Sheets agenda times being rejected because Sheets displays `08:00` and `09:30` as `8:00` and `9:30`. Release 15 normalises one- or two-digit 24-hour inputs before validation/import. The regression passed, followed by the production build and 33 of 33 automated tests, ESLint, and a 0-vulnerability production dependency audit.
- Public Sites version 15 deployed successfully from exact commit `8ce3bd474fd7200be7ac2ef6068e2b430a9af1af`. The same live Master preview then passed with 0 validation issues, 0 pending writes, 0 failed writes and 0 unintended archive impacts.
- Fresh live app-to-Sheet acceptance passed 19 of 19 checks: exact-version pull/apply handled 2,110 current records with 0 archives; one contact-free disposable guest was created, updated and archived; all three states were read directly from Google Sheets; the removed row left active app workflows; final connector state was 0 pending / 0 failed.
- Fresh live Sheet-to-app acceptance passed 14 of 14 checks: a contact-free Master guest reached preview, exact-version apply and the active Guest snapshot; marking only that row removed produced exactly 1 preview impact identifying that guest, confirmed apply archived it, and the final active snapshot contained 0 matches. A separate unknown-category write was rejected with HTTP 409. Final connector state was 0 pending / 0 failed, and a final read-only preview reported 0 remaining impacts.
- 2026-08-22 UI/aesthetics cleanup block started. `docs/HANDOFF.md`'s documented live URL (`tech-sinisters.chatgpt.site`) returned "Employee number or PIN is incorrect" for the owner's real credentials; the owner's phone resolves to a different URL, `yil-golden-jubilee-event-ops.yil-jubilee.workers.dev`, where the same credentials succeeded. `HANDOFF.md` updated to record both, with the workers.dev URL marked confirmed-working and the Sites URL marked unconfirmed. Root cause of the two URLs (stale Sites deployment vs. current one) not yet investigated.
- Post-login/resume landing-screen fix (owner report: reopening the app sometimes lands directly in Event Work/Guest Coordination instead of the work-area chooser). Code inspection of `app/EventOperationsApp.tsx` found every existing path (fresh sign-in, first PIN change, session-restore-on-reload) already computes and sets the chooser screen — confirmed live on the corrected URL: fresh login 1/1, full page reload while signed in 1/1, both landed on the chooser. The only remaining gap was an installed-PWA resume without a full reload, where React state simply persists unchanged. Added a 5-minute background-duration check (`visibilitychange` handler + timestamp) that resets to the chooser only after a real absence, so brief backgrounding (e.g. the in-app camera/photo-picker flow) is unaffected. `tsc --noEmit`: 0 errors. `npm run lint`: 0 errors/warnings. `npm test`: production build succeeded, 33 of 33 automated tests passed (unchanged from before this fix — no existing test covers this new behaviour yet). Functional check on the local, DB-free "Open local Event Work preview" build: 2 of 2 short-background cases (elapsed ~0 ms) stayed on the current screen; 2 of 2 long-background cases (elapsed 6 simulated minutes, one from Event Work and one from Guest Coordination) correctly reset to the chooser. Not yet verified: deployed build, or the real installed PWA on a phone with a true background/foreground cycle.
- Consistent safe-area-inset handling. The fixed header, committee strip and bottom tab bar previously ignored `env(safe-area-inset-top/bottom)` (only the login panel handled it); added `--safe-top`/`--safe-bottom` CSS custom properties and applied them to `.eventHeader`, `.committeeStrip`, `.eventTabs`, plus matching bottom clearance on `.eventBody`/`.toast` on phone widths. `.guestHeader`/`.guestTabs`/`.guestCommitteeStrip` inherit the same base classes, so Guest Coordination is covered without separate changes. `tsc --noEmit`: 0 errors. `npm run lint`: 0 errors/warnings. `npm test`: build succeeded, 33 of 33 automated tests passed. Verified on the local preview build that computed styles resolve the safe-area custom properties correctly with a 0px fallback (no layout regression on a non-notch viewport: header stayed 62px, tab bar padding stayed 0px). Not verified: actual rendering on a real notched/home-indicator device — headless/emulated browsers don't simulate `env(safe-area-inset-*)`, so this specific claim stays unproven until checked on a real phone or a simulator that reports non-zero insets.
- Back/Forward fixes (owner report: "back screens are also not done correctly"). Two fixes landed: (1) the Event Work search panel (`EventSearch`) now uses the existing `useSheetHistory` hook, so Back closes it instead of leaving the current tab — reproduced 1/1: opened search, triggered Back, panel closed and tab was unchanged. (2) A confirmed regression where switching Event Work tabs (or Guest Coordination tabs, or "Switch work area") while a Job/Budget/Send/Sheet-sync sheet was open left the sheet's own pushed history entry orphaned, so one Back press did two things at once. `navigateScreen`/`openSectionWithHistory`/`navigateEventTab` (Event) and `navigateGuestTab` (Guest) now close any open sheet by absorbing its history entry (`replaceState` instead of `pushState`, explicitly stripping the leftover `yilSheet` flag) rather than stacking a new entry on top of it. First attempt at this fix only spread the old state without deleting `yilSheet`, so the sheet's own cleanup effect still saw the flag and popped the just-written entry straight back off — caught via a live functional check on the local preview build, not by the automated suite (no test covers this path yet), then corrected. Reproduced 1/1 on the corrected code: opened a Job sheet from Home/Programme section, switched to the Budget tab — sheet closed, Budget rendered immediately, `history.state` had no `yilSheet` remnant; one subsequent Back press returned cleanly to the Home/Programme section in a single step. `tsc --noEmit`: 0 errors. `npm run lint`: 0 errors/warnings. `npm test`: build succeeded, 33 of 33 automated tests passed (smoke-checks `window.history.pushState`/`popstate`/`replaceState` substrings exist in source — required switching the fix from computed-property history calls to explicit `if/else` to keep passing). Not yet done: the in-page segmented-view Back-stops (Programme/Jobs/Prep toggle, Malur/Taj toggle, Guest Mine Agenda/Travel toggle, date/group pickers) from the original Module 2 plan, and the equivalent orphaned-entry gap in Guest Coordination's per-view sheets (GuestEditSheet/TravelSheet/StaySheet/AgendaSheet), which are local to child view components unmounted by a tab switch rather than hoisted like JobSheet/BudgetSheet — same latent phantom-history-entry class, but without the visible sheet-stays-open symptom, deferred as a smaller residual for a follow-up pass. Not verified on a deployed build or a real phone.
- Systemic UI consistency pass (Module 5), following a 17-agent read-only verification workflow that checked the prior audit's claims against live cascade-resolved CSS rather than source presence. Finding: a "one list language" CSS pass had added matching borders/radius/shadow to 7 of 10 row selectors, which read as unification in a diff, but left `min-height`/padding untouched on 4 of 6 row types and the row-height spread (52-88px) was completely unchanged — the audit doc itself had been fooled by the same diff. Fixed, verified live on the local preview build after each change: (1) row height unified to 60px across `.jobRow`/`.jobMain`, `.prototypeSectionCard`, `.programmeRows>button`, `.budgetList article`, `.guestList .guestRow`, `.mineLineList article`, `.mineTravelList>button` (Updates kept intentionally taller — `min-height:auto` — since it holds 3-4 lines of real content, not 2); (2) `.jobRow`'s box-shadow, which a later "Round 4" pass had regressed to a second recipe, restored to match the other six; (3) `.travelCard`/`.templateList article` brought onto the same 14px-radius/shadow recipe (was 16px/12px, a third and fourth value); (4) a real, previously-undiscovered bug: Guest Travel's stop list (`<ol className="routeLine">`) collided with an unrelated, orphaned CSS block meant for a different, since-removed JobSheet layout — `border-top:0!important` was force-removing row dividers and `grid-column` rules were pushing time/place text into the wrong columns with a spurious timeline dot; deleted the dead CSS, which was the entire fix (Travel never needed that block, it only collided with it) — reproduced 1/1: grid-template-columns now the correct `72px auto`, border-top a normal `0.8px solid`, no pseudo-element dot; (5) `.eyebrow` given a real base rule (11px, uppercase, letter-spacing) after confirming zero bare `.eyebrow{}` rule existed anywhere — the Home hero tagline explicitly opted out (`text-transform:none`) since it's a sentence, not a label, and the owner had already chosen "keep 1 line but make it look intentional" for it specifically; (6) `.eventApp`/`.eventBody` given the panel/gutter treatment from `docs/UI-DESIGN-DIRECTION.md` §1 (distinct `var(--paper)` background, `border-radius:20px 20px 0 0`, gutter 14px→20px / 12px→16px) — confirmed this had never been applied despite being proposed; (7) `.pickerChip summary` (every filter-dropdown chip app-wide) restored from 40px to 44px — a real touch-target-floor regression from the prior session's own "kept everything ≥44px" pass, caught by the verification workflow, not self-caught; (8) `.venueSegments`/`.mineSegments`/`.choiceRow` buttons unified to 48px (`.eventSwitch` intentionally kept its own larger 60px/52px sizing — richer two-line content, not the same control class). Also removed confirmed-dead CSS: `.groupCard`/`.groupGrid`, `.budgetHero` and its sub-rules, the orphaned two-column `.venueHero`/`.venueMetrics`/`.venueGrid`/`.timeline`/`.venueJobs` layout remnants, `.mineGroupPicker>.eyebrow`, `.heroAttention`/`.heroReady`, `.mineRemove` (already unused since the prior session's delete-button fix), and fixed `.staysSearch`'s dead third grid column (`1fr auto auto` → `1fr auto`, only 2 children ever render). `tsc --noEmit`: 0 errors. `npm run lint`: 0 errors/warnings. `npm test`: build succeeded, 33 of 33 automated tests passed. Verified live on the local preview build, screen by screen, after the full pass: Home (card 60px, panel background/radius, tagline full-text/fits/no-uppercase, section eyebrow uppercase), Malur (row 60px, correct shadow, segment 48px), Budget (row 60px), Updates (row `auto` height, 14px/16px padding), Guest Mine (panel, row 60px, segment 48px, filter chip 44px, eyebrow uppercase/11px), Guest directory (row 60px, switch 52px), Guest Travel (routeLine fix confirmed via computed grid-template-columns/border/pseudo-element check), Guest Stays (toolbar column fix confirmed at source; mobile collapses to single column regardless, as before). Not yet done: the screen-specific findings from the same verification pass that are genuine taste/judgment calls rather than mechanical fixes (e.g. Budget's "cancelled" status has no distinct color, Login's card-radius/shadow doesn't match its two closest siblings, Login has no brand wordmark) — held back for the owner to decide rather than guessed at, after two prior rounds of guessing wrong on visual taste (gold badge circles, "your group" dropdown). Not verified on a deployed build or a real phone.
- Module 5's own judgment-call items decided by the owner directly: card shape unified to the curved (radius+shadow) treatment; Budget's "cancelled" status left without a distinct color (explicitly deferred by the owner, "will see afterwards"); Login given a wordmark (`<span class="miniMark loginMark">YIL 50</span>`).
- Sheets/modals consistency pass (Module 6), following a second, 8-agent read-only verification workflow scoped to exactly what Module 5's tab-by-tab review had not covered: every sheet/modal component (JobSheet, BudgetSheet, GuestEditSheet, StaySheet, TravelSheet, TemplateSheet, SendSheet, GoogleSheetsSyncSheet, AgendaSheet) plus re-verification that Module 5's fixes actually held and completion of the Invitations screen, which had failed with a connection error in the first workflow run. Findings and fixes: (1) every sheet's "‹ Back" button computed to 40px, not the 44px `touch-targets.css` intended — a higher-specificity `.jobSheet>header>.sheetBack`/`.sheetBack` rule in `globals.css` was silently winning regardless of file load order; added the matching 44px `min-height` rule at the winning specificity, app-wide; (2) BudgetSheet's validation error rendered muted gray, not red — `.loginForm .formStatus` (compound, higher specificity) was beating `.errorText` (single class); added `.loginForm .formStatus.errorText{color:var(--danger)}`; (3) BudgetSheet was the only sheet missing the "‹ Back" control every other sheet has; added it to its `<header>`; (4) Module 5's dead-code removal was incomplete — `.groupCard`/`.heroAttention`/`.heroReady`/`.mineRemove` fragments were still present inside `@media(max-width:380px)` blocks in `guest.css` even though their base rules had already been removed; deleted the remaining fragments; (5) `.stopEditRow`'s grid column width disagreed between `guest.css` (88px) and `touch-targets.css` (84px, silently winning at the same specificity by file load order); aligned both to 88px; (6) six more 12px-font-size-floor violations (`.dangerAction`, `.channelChoices small`, `.agendaEditRow label>span`, `.templatePurpose>span`, `.selectedGuest>span`, plus `.stopEditRow label>span`) and one 44px-touch-target gap (`.preflightPanel summary`, previously unsized) fixed; (7) `.checkRow`/`.mediaPicker` brought onto the app's 60px row-height target (was 58px); `.sheetBody` given a shared 14px base font-size (previously only the guest-side sheets had it, via a narrower selector); `.stopEditRow`/`.agendaEditRow` input/button heights unified to 50px (previously 46/48/50px — three different values for the same input pattern across two files) — this required updating `tests/rendered-html.test.mjs`, which was pinned to the old exact pixel values (46px/48px) from a past historical fix rather than a deliberately-chosen target; (8) `.plainRule`'s border-left brought to 3px, matching the other four gold-border note components (was the only one at 4px). `tsc --noEmit`: 0 errors. `npm run lint`: 0 errors/warnings. `npm test`: build succeeded, 33 of 33 automated tests passed (after the height-value test update in item 7). Verified live via computed-style checks on the local preview build: `.sheetBack` resolves to 44px `min-height` on BudgetSheet's actual rendered DOM; the "‹ Back" button is present in BudgetSheet; `.formStatus.errorText` resolves to `rgb(164,56,49)` (the `--danger` token), not the previous gray. Landed as PR #6 (`ui-fixes/module-6-sheets-and-cleanup` → `codex/event-work-foundation`), merged and synced. Not verified: exact validation-error text on a real failed submit (synthetic test submits were intercepted by native HTML5 form validation before reaching the styled error path — the color-cascade fix itself is proven directly via computed style, independent of triggering that specific submit path); deployed build; real phone.

- 2026-08-23 direct Cloudflare deployment from the owner's own account (wrangler OAuth login clicked by the owner; no credentials shared in chat). Built at commit `9e1797c57e7e` (all 13 merged UI PRs), patched the generated `dist/server/wrangler.json` placeholders to the real `yil-jubilee-db` D1 id and `yil-jubilee-media` R2 bucket, deployed with `--keep-vars`; all pre-existing worker secrets (SESSION_PEPPER, WhatsApp/Acele values, allowlist) confirmed present and preserved. Live verification: root HTTP 200 referencing the new `yuken-50-badge.webp` (served, 44,454 bytes — today's asset), service worker cache stamp `yil-event-shell-9e1797c57e7e` (exactly today's merged commit), and unauthenticated Event/Guest/Budget APIs denied 3 of 3 with HTTP 401. Live URL: `https://yil-golden-jubilee-event-ops.yil-jubilee.workers.dev`. This establishes a repeatable owner-account deploy path independent of the earlier Sites publishing flow.

- 2026-08-23 Master Sheet event-tab gates installed and proven live. A one-time Apps Script (new project bound to the sheet; the workbook previously had zero protections of any kind) added, for the event tabs only per the owner: warning-mode protection on START HERE and on the title/header rows of 1 People, 2 Sections and 3 Jobs; Yes/No dropdowns for the remove/core columns; 3 Jobs dropdowns fed live from 2 Sections headings and 1 People initials plus a Malur/Taj/Both/General venue list and a real-date rule on FINISH BY (all warning-mode, so unusual-but-valid entries are flagged, never blocked; the app import stays the final gate); and a red incomplete-row highlight. Reproduced 2 of 2 gate behaviours on a scratch row: a title-only row turned red, and venue "Bangalore" got the invalid-entry warning triangle; the scratch row was then removed. Guest tabs (4-10) deliberately untouched. The sheet remains owner-editable throughout; the connector script was untouched (it is not bound to this spreadsheet).

- 2026-08-23 Master Sheet auto-reflect deployed and proven live end-to-end. New cron trigger (*/10) on the production worker self-invokes /api/master/auto-sync with a machine secret; unauthenticated calls denied 1 of 1 with HTTP 401. The deploy step first exposed a real regression: the wrangler-managed worker had no GOOGLE_SHEETS_WEB_APP_URL/SHARED_SECRET at all (they lived only in the old Sites environment), so even manual sheet sync had been silently broken since the deploy-path change - reconnected by reading the connector /exec URL from the owner Apps Script deployment and rotating a fresh shared secret set on both sides via clipboard/file channels so no secret value entered the session log (one screenshot showed a 40-of-64-char prefix of the new value; remaining entropy 96 bits, re-rotation trivial if desired). First two production runs behaved exactly per design and are logged in the new sync_runs table: run 1 outcome unstable (first content sighting, correctly waited), run 2 stable -> previewed -> found 43 archive impacts from real sheet-vs-database drift and correctly refused to auto-apply, queueing awaiting-approval for the core one-tap gate (banner live in the app). Unit gates 37 of 37 including 4 new safety-rule tests (new-incomplete row set aside, existing-invalid row blocks, valid payload untouched, content hash ignores export stamp but not edits). The approval of the 43 pending archives is an owner data decision, deliberately not taken by the agent. Note for future deploys: the wrangler.json patch step must include the cron triggers block.

- 2026-08-23 final Event-section audit and fix program. A two-round adversarial audit (six specialised finders; every finding re-verified against current code before acceptance; the first run's own verification cap was caught silently dropping 25 findings and a dedicated second verification round was run) confirmed 41 real findings and refuted 3. All fixed in one program (PR #20) and deployed as release ee42927e9920: depth-based sheet history (stacked search+sheet Back now closes one layer per press - reproduced live at depths 1/2 before and after; tab-switch-with-search bounce fixed and proven; stale sectionId Back regression fixed and proven with the exact broken sequence; resume-reset protected from open guest sheets), master-sync hardening (removed-reference jobs now raise validation issues instead of vanishing silently at apply; applied-state integrity when snapshot/baseline fail after apply, with baseline retry; atomic single-flight lock; manual pull-applies recorded for no-change detection; banner state from direct query not a 10-row window; cron failures always write an error row), type-scale and touch-floor consistency across all five tabs (12px floor in sheet internals and Budget rows, 11px uppercase-micro standard, back link and mobile search raised to 44px; committee-strip circles and O/C chips documented as deliberate compact exceptions), and dead-code removal (40 never-rendering CSS declarations verified by cascade analysis, unused requireCore and TemplateUi.status removed, canReassignJob now enforced in the assignments route rather than existing only in tests). Gates after the program: tsc clean, ESLint clean, 37 of 37 tests. Live deploy verified: root 200, service-worker stamp ee42927e9920, unauthenticated API 401.

- 2026-08-23 Guest-section consistency audit (owner-requested, same discipline as the Event program). A 4-finder workflow was clipped by a session limit after one finder completed; its 10 dead-CSS findings were all adversarially verified (0 refuted) and the remaining three dimensions (tab consistency, sheets, type/touch floors) were audited inline with the same cascade-analysis method. Fixed in PR #22 and deployed as release bf8d24ed4845: 14 dead guest-side CSS clusters removed (guestSectionTitle, the vestigial filterRow native-select layer, dead 9px pill halves, guestHero dead sizing, the phantom mineLineList remove column plus the noRemove class, inviteCard figcaption, choiceRow b, guestLensBar, guestMetrics no-ops, four identical-value duplicates); guest list headers aligned to .prototypeSectionTitle (verified live on Travel); 12 type-floor violations raised to the 12px floor with numeric/uppercase micro-badges documented at 10-11px by design (two raises verified live at computed 12px); sheets dimension clean (all 8 guest sheets share the one Sheet component with the full header pattern) and touch floors clean (every guest control >=44px). Gates 37 of 37; live deploy verified root 200 / API 401.

- 2026-08-24 Guest functional-completeness audit and fix program (owner-requested: required entity bindings and dead-end flows, e.g. "travel must attach to a group or category"). A 5-finder + verify-all workflow (39 agents, 0 errors) produced 34 findings, every one adversarially verified: 27 confirmed, 7 refuted (among the refuted: travel-plan archival paths and in-app hotel/category creation, which do exist via Master Sheet ownership). The owner's named example was confirmed already triple-enforced (disabled save button, HTTP 400 on empty categories, active-category 409). All 27 confirmed findings fixed in one program: Mine-tab travel sends now bind the whole day with each guest matched server-side to the plan covering their category and the plan's own event driving invitation checks and variables (previously only the first vehicle of the day was sent and the event was inferred from the date, so a second vehicle or an added day silently skipped guests); TravelSheet gained a Malur/Taj picker and disables categories already covered by another plan for the chosen event+date, naming the conflicting plan (server 409 also names the categories now); non-core senders are scoped server-side to their own groups for every message purpose (previously only agenda sends were checked; travel/stay audiences could reach the whole guest list); phone numbers are validated and normalised at guest save and re-validated at preflight (previously "TBD" counted as a reachable contact and was silently mangled at send); the send loop re-checks guests.active per recipient (previously a guest archived between review and send was still messaged); invitation and travel preflights skip not-invited guests with a clear reason (previously discovered per-recipient at send, after the confirm dialog); stays can be removed (new DELETE with full audit; remove button in the sheet), stays pointing at Master-Sheet-removed hotels are excluded from ready pools and flagged in the list/sheet with the real hotel name (previously they stayed "ready" and the message named the removed hotel); interrupted send batches now surface in a banner with per-recipient failure reasons and a resume that sends only never-reached recipients (previously a stranded batch was unreachable and the visible retry path double-sent); a delivery-configuration check runs before confirm with a test-mode note (previously 40 recipients each burned a failed attempt to discover the provider was unconfigured); guests can be invited to or removed from either event via checkboxes and coordinators can record phone/in-person RSVPs per event with audit (previously the second event was unreachable from the app and verbal replies stayed "Awaiting reply" forever); agenda lines gained the details field end-to-end (edit sheet input; server agenda_lines now carries details so the preview matches what is sent); saving an empty agenda asks before wiping the day; every guest-side sheet now closes only after a successful save (class fix across guest, stay, agenda and travel savers — previously a rejected save looked like success); machine reason codes and template variable names replaced with plain-language instructions; category selection requires an explicit choice with a no-categories explanation (previously defaulted silently to the alphabetically first); car-mode stops capped at 20 in the editor; budget entries gained PATCH + a prefilled edit sheet so status can walk planned→approved→paid and typos can be corrected without duplicate rows. Gates: tsc 0 errors, ESLint 0 errors, 37 of 37 tests (one test updated for the deliberate travel-contract change), production build clean. Live checks on the local preview build: guest sheet shows both event checkboxes and both RSVP recorders 1/1; travel sheet shows the event picker and a live conflict-disabled category naming its plan 1/1; agenda details field spans its own row 1/1; budget row tap opens the prefilled edit sheet 1/1.

- 2026-08-24 the functional-audit program (PR #24) merged and deployed as release `6de358310af1` (worker version e8cbfdf2) with the D1/R2 bindings and the */10 cron schedule preserved by the config patch. Live verification: root HTTP 200, service-worker stamp `yil-event-shell-6de358310af1` matching the exact merged commit, and unauthenticated snapshot, budget and the new message-batches API all denied 3 of 3 with HTTP 401.

- 2026-08-24 full end-to-end acceptance run on the live app plus the real Master Sheet, everything except message/email sending. The Master Sheet had been emptied on 23 Aug (all data tabs left with only their header rows); restoring the 21 Aug 18:15 version brought the real dataset back and the CHECKS panel now reads 23 people, 13 sections, 141 jobs, 12 guest categories, 10 guest groups, 1,855 guests, 6 agenda lines, 2 travel plans, 4 travel stops, 5 hotels. Auto-sync then reported `needs-fixing` with 2 problems; a temporary formula panel located them exactly (Group Agenda rows 11-12 naming a group that exists only in the dummy database, written into the sheet by an app-side agenda push while database and sheet held different datasets). Those two rows were cleared and the sheet now validates clean: live pull-preview returns HTTP 200 on the full 1,855-guest workbook in 14.2-19.0 s across three runs, reporting 44 dummy-record removals held behind the core-approval gate and explicitly preserving jobProgress 9, guestInvitations 16, guestStays 8, messageRecipients 21. Live app checks passed 27 of 27: wrong PIN refused and correct PIN admitted; chooser landing; section detail; activity sheet at history depth 1 with one-Back-one-layer proven twice; update posted, listed in Updates and reopened as a read-only post view; Malur/Taj heroes and all three venue segments; Budget add then tap-row then prefilled edit then status advanced to cancelled; search open and closed by Back; Guest Mine agenda line saved with the new details field and shown in the list; travel sheet event picker plus a live conflict-disabled category naming its plan; guest add refusing the phone "TBD" then accepting a spaced number; both event checkboxes and both RSVP recorders present, persisted after reopen; stay assigned, listed, then removed; invitation metrics, Taj card preview, 6 templates; sync sheet showing outbox 0 pending / 2 delivered. Permissions were proven live by temporarily demoting the test login to non-core: Budget GET and POST, Master Sheet sync, sync status, a foreign group's agenda write, a foreign guest's message preflight, a foreign activity edit and a reassignment were all refused (7 of 7 HTTP 403) while the guest snapshot stayed readable; the preflight refusal is today's new scoping fix confirmed in production. PWA surface verified live: manifest with standalone display, 192/512 and maskable icons, theme and background colours, apple-touch-icon and status-bar style, viewport-fit=cover, one activated service worker, a precached /offline page returning 200, and zero API responses in any cache. Measured scale facts: 468 bytes per guest in the snapshot, so ~848 KB uncompressed at 1,855 guests; the guest list rendered 6 DOM nodes per row with no cap, projecting ~11,100 nodes on first paint - fixed by capping Guest directory and Invitations at 60 rows with a narrow-down line, matching the cap Stays already used, while counts, metrics and send audiences stay uncapped. Import scale confirmed sound by reading `src/server/import-master.ts`: every table is written through one `json_each` statement, so row count does not multiply statements, and existing people keep their PIN because the ON CONFLICT clause updates neither pin_hash nor pin_salt (only new rows receive INITIAL_LOGIN_PIN). Four code fixes landed with this run: app-created guests can now reach both events (the invitation list is built over both events instead of mapping only existing rows, which previously made the second evening unreachable for a guest added in the app); validation messages now quote the offending value; reported problem lines now carry the permanent record ID; and the sync screen lists the actual problems and the waiting-row count instead of only counting them. A permanent CHECKS tab was added to the workbook - read-only formulas giving row counts per tab, twelve must-fix cross-tab and duplicate checks that name the offending rows, and three advisory lines - and it currently reads ALL CHECKS PASSED with advisories: 2 guest groups have nobody in charge, 68 of 1,855 guests have neither WhatsApp nor email, and 4 rows are marked Remove = Yes awaiting the next sync. The extra tab was confirmed harmless to the connector (identical source version hash before and after). Gates: tsc 0 errors, ESLint 0 errors, 37 of 37 tests, production build clean; deployed as release `39212d0e0be1` with D1, R2 and the */10 cron preserved, root 200 and the service-worker stamp matching. Every test artefact was removed afterwards: the app-created guest row was cleared from the sheet (row 1859), and the QA budget entry, agenda line, activity update and guest record were deleted from the database; the temporary core login was deactivated with its PIN and sessions destroyed rather than deleted, so its 11 audit events survive, and a login attempt with its old credentials now returns HTTP 401. Not verified: an actual apply of the real 1,855-guest workbook into the database (deliberately left for the owner - it archives the 44 dummy records), UI behaviour with 1,855 guests actually present, and messaging/email delivery which was out of scope for this run.

- 2026-08-24 second block: the three named gaps from the acceptance run were closed, and a lockout hole found while preparing the real-data apply was closed first. The hole: applying the Master Sheet archives every person absent from it, so all current sign-ins would end, and the 23 sheet people would arrive as new rows holding INITIAL_LOGIN_PIN — with no recovery, because the import deliberately never overwrote an existing person's PIN, so rotating the starting PIN and re-syncing could not restore access. Two changes fix it. First, the import's conflict clause now refreshes pin_hash and pin_salt only when must_change_pin=1, so a PIN somebody chose is still untouchable while a never-used starting PIN can always be rotated and re-applied. Second, a core-only "Sign-in PINs" screen (account menu, Event Work) lists everyone who can sign in with their state — PIN chosen, not set up yet, or locked out — and hands out a fresh random four-digit PIN that is shown exactly once, forces a change on first use, clears any lockout and signs that person out everywhere; the new PIN is never written to the audit log, and GET /api/people returns only a hasPin boolean, never the hash or salt. Proven live end to end: a PIN handed out through the screen signed that person in (HTTP 200, requiresPinChange true, correct name) while a wrong PIN still returned 401. Second gap closed: an update posted by mistake was previously permanent, with no edit or delete anywhere. PATCH and DELETE on /api/jobs/[id]/updates/[updateId] now let the author or a core member correct the wording or withdraw the post; withdrawing removes the attachments from private storage only after the rows are gone, and the original wording is kept in the audit log. Verified live: correcting replaced the text and the old wording disappeared, then withdrawing removed the post entirely. Third gap closed: the finish-by date is now settable in the activity sheet by core members (the field was previously sheet-only), rejects anything that is not YYYY-MM-DD, treats an absent field as "no change" rather than "clear", and refuses non-core callers with HTTP 403 — verified live by setting a date, saving, reopening and reading it back. All three new operations write audit rows (credential.pin-reset, job.update-edited, job.update-withdrawn — one each, confirmed in live D1). A migration added job_updates.edited_at and was applied to the live database. Gates: tsc 0 errors, ESLint 0 errors, 42 of 42 tests (5 new tests covering the PIN-refresh conflict clause, the core-only and no-credential-in-audit rules for handing out a PIN, the author-or-core rule and delete ordering for updates, and the core-only finish-by rule), production build clean; deployed as worker version 2ec47618. Owner-delegated decision taken: the two guest groups with no coordinator were given a lead and a backup from the two people whose recorded responsibility in the sheet covers customers, so their guests now appear in someone's Mine tab; the CHECKS panel's "nobody in charge" line reads none and both coordinator-initials checks pass. Owner-delegated decision deliberately NOT taken: the real 1,855-guest workbook was not applied, because the same message that delegated the decision also asked that only test data be used; the app database still holds only the dummy set, the 44 pending removals remain queued behind the approval gate, and the apply is now recoverable whenever it is wanted. Test artefacts removed again afterwards: the temporary core login was deactivated with its PIN and sessions destroyed (audit trail kept, login attempt returns 401) and its local PIN file deleted. One consequence to hand over: the dummy sign-in for employee number 1206 had its PIN reset during the live proof, so it now carries a temporary PIN and will demand a new one on first use.

- 2026-08-24 third block: exhaustive screen-by-screen sweep of the live app with timings, plus two changes the sweep forced. Owner correction recorded: the app serves 40-100 event coordinators, not 1,855 people — 1,855 is guest data volume, not user load, so earlier scale worry was overstated. Measured API response times on the deployed worker, all healthy: login 170 ms, auth/me 75 ms, event snapshot 210 ms, guest snapshot 222 ms, budget 137 ms, people 132 ms, templates 133 ms, sync status 168 ms, message batches 117 ms; wrong PIN and short PIN both refused in 22-226 ms. Screen load times: Updates 1.32 s, Malur 1.32 s, Taj 1.31 s, Budget 1.42 s, venue segments 0.71 s each, section detail 1.41 s, activity sheet 1.31 s, PIN screen 2.72 s (list fetch included). Scroll behaviour verified: a 269 px scroll reset to 0 on tab change, and every screen opens at the top. Back paths verified one layer per press: search closes leaving the tab intact, activity sheet closes leaving the section, section closes returning Home, budget sheet closes, PIN sheet closes leaving Budget visible. Full CRUD exercised end to end on live data: budget add then tap-row edit (250.50 planned to 999.99 paid, totals moved correctly), guest add refused without a category then created, guest edited to both evenings with an RSVP recorded and both persisted on reopen (`events=true,true rsvp=accepted` — the app-created-guest second-event fix confirmed on a real record), travel plan created then route edited then the conflict guard immediately disabled the category naming the new plan, stay assigned then removed, agenda two lines with details added then one edited then one deleted then the empty-save confirm shown and cancelled with data intact, and the send sheet opened to review and closed without sending anything. PWA verified for both platforms: Android install prompt supported (beforeinstallprompt present), iPhone add-to-home supported (apple-touch-icon plus status-bar style), mobile-web-app-capable set, icons 192/512 plus 512 maskable for Android adaptive icons, standalone display, theme colour matching in manifest and meta, and a 2-second background trip does not lose the coordinator's place. Two changes the sweep forced. First, owner decision reversing an earlier one: the work area is now remembered, so reopening goes straight back to Event Work or Guest coordination instead of asking again; the chooser appears only on first ever use or from "Switch work area", and a long absence now refreshes the data in place rather than bouncing the coordinator to the chooser. Second, a real defect reproduced twice in one day and now fixed at the root: app-side writes push group and category references by NAME, so while the app and the Sheet hold different datasets an app edit writes a row naming a group that exists only in the app, and that single row then blocks the entire sync (`needs-fixing`, nothing flows). The outbox now refuses to push while a Master version is awaiting approval or needs fixing, keeping the writes queued instead of poisoning the Sheet — verified live, the push returned `heldForMasterReview: true` with 0 attempted. Worth recording: the per-row conflict guard already caught two of these writes with "Sheet row changed since the last confirmed pull", and the new CHECKS panel located the two bad rows instantly ("row 11, row 12"), which is exactly why it was built. Gates: tsc 0 errors, ESLint 0 errors, 42 of 42 tests, build clean; deployed as worker version cc9917ca. Every sweep artefact removed afterwards from both sides: the Sheet is back to 1,854 guests with 0 sweep plans, 0 sweep stops and 0 dangling agenda rows, the database is back to exactly 8 people and 8 guests with an empty outbox, and the temporary login is deactivated with its PIN destroyed (login attempt returns 401).

- 2026-08-24 fourth block: three testing methods the earlier click-through sweep could not have found things with, run after the owner asked how bugs could be reduced rather than just re-run. Method one, a permission matrix across all 27 API routes (methods x isCore x group scope x owner scope), found three guest-side write paths carrying no authorisation beyond "is signed in": travel plans (PUT), stays (PUT and DELETE) and guest records (POST, PATCH, DELETE). Any coordinator could add or edit any travel plan, assign or clear any guest's hotel, and add, edit or archive any guest — a direct violation of the recorded rule that a non-core employee manages only assigned guest groups. Fixed with three helpers in `src/server/permissions.ts`: `coordinatedGroupIds` (active groups only), `mayManageGuest` (core, or the guest sits in one of their groups; a guest with no group fails closed) and `mayManageTravelCategories` (core, or every guest reached by the plan's categories is theirs). A travel edit is measured against BOTH the plan's current and its submitted categories, so a shared plan cannot be taken over by narrowing it. Proven live with a real non-core session: editing another group's guest, archiving one, creating a guest with no group, assigning a hotel, clearing a stay and editing a shared travel plan were all refused 6 of 6 with HTTP 403, while reading everything still worked. Method two, adversarial probing of the live API as that non-core user: privilege fields injected into a request body (isCore, active, forged id) were ignored and the request still refused; budget write, PIN reset and the people list all refused 403; a SQL-style string in a name field was rejected and the guests table was intact afterwards; a 200,000-character name was refused 400; invalid language and event enums were refused 400; neither the guest snapshot nor auth/me contained any credential material; and the session cookie is not readable from JavaScript. Brute force was measured directly: four wrong PINs then HTTP 429 "Too many attempts, wait 15 minutes", and the correct PIN was still refused while locked. Method three, a static audit against a public pre-launch checklist: no `dangerouslySetInnerHTML` or `innerHTML` anywhere, so React escapes all user content; every SQL value is a bound parameter (the only interpolations are hardcoded table names); length caps exist on both client and server; `npm audit --omit=dev` reports 0 vulnerabilities; and git history contains no secret values, only empty placeholders in `.env.example`. Uploads were already strong and are now locked by tests: at most 3 attachments, 10 MB per photo, 50 MB per update, an eight-type allow-list, magic-byte signature verification so a renamed file is rejected, and filenames stripped to their final path segment and capped at 180 characters. The one real gap the probe found was that every browser-side security header was missing; all responses now carry Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options DENY, Content-Security-Policy frame-ancestors none, Referrer-Policy, Permissions-Policy and Cross-Origin-Opener-Policy, applied by wrapping the worker's handler so no route can miss them, with 101/204/304 passed through untouched and any header a route sets itself left alone. `frame-ancestors` was chosen over a script policy deliberately: the app is server-rendered with inline bootstrap scripts, so a script-src rule would need 'unsafe-inline' and buy nothing. Verified live on both a page and an API response, and the app, service worker, manifest, icons and login artwork all still load with no page errors. Gates: tsc 0 errors, ESLint 0 errors, 51 of 51 tests (9 new across guest write permissions, security headers, upload limits and login lockout); deployed as worker version 4bb460c7. Probe artefacts removed and the temporary login retired again: 8 people, 8 guests, empty outbox, login attempt returns 401.

- 2026-08-24 fifth block: load measured, oversight built, and a privacy gap closed. Stress test against the deployed worker with a real session, zero errors at every level: the heaviest read (guest snapshot) served 10 simultaneous coordinators at 520 ms median, 25 at 747 ms, 50 at 1.42 s, 100 at 3.28 s (p95 3.50 s); event snapshot 100 at 2.03 s; auth/me 100 at 352 ms; cold page load 50 at 103 ms median. The owner's real figure is 40-100 coordinators spread over time rather than in one instant, so normal use sits in the 0.5-1.5 s band. The test exposed a data-minimisation problem rather than a performance one: `app/api/guest/snapshot` returned every guest's phone number and email to every signed-in coordinator, so with the real 1,855-guest list the entire contact database would land on roughly 100 phones, including coordinators who may only edit one group. Contact values are now returned only for guests in the groups that person coordinates (core still sees everything); names, companies, categories and groups remain visible so the directory still works, and a new `hasContact` boolean keeps readiness counts honest without exposing the value. Proven live with a non-core session: 8 guests visible, 0 contact values received, 8 withheld, reachable count still correct at 4. New oversight surface for the committee, answering the owner's question about monitoring: `GET /api/overview` (core only, read-only — tests assert it performs no insert, update or delete) plus a "Where we stand" screen in the account menu, gathering what other screens already own so it can never disagree with them. It shows both evenings with days-to-go and the RSVP split, event-work progress overall and per section as bars, anything blocked with its note, gaps still to arrange (vehicles missing details, guests with no way to reach them, guests with a room), the Master Sheet state including any problems, and a plain-English feed of the 25 most recent changes with who made them and how long ago. The audit trail already recorded everything needed — every login, job update, guest change, stay, agenda edit, budget change, template approval, message acceptance, sync and PIN action — it simply had no window into it; the feed deliberately excludes logins and refusals so it stays a record of what changed. Verified live: HTTP 200 in 373 ms returning work 1/9 complete across 4 sections, Malur 83 days out with 7 invited and 1 attending, Taj 86 days out with 5 invited, travel 1/1 ready, 4 guests unreachable, sync awaiting-approval, and 25 activity entries; a non-core caller is refused 403. Gates: tsc 0 errors, ESLint 0 errors, 54 of 54 tests (3 new over contact withholding, the overview being read-only and core-only, and every reported action having plain-English wording); deployed as worker version 844196eb. Stress artefacts removed and the temporary login retired: 8 people, 8 guests, empty outbox.

## Not complete

- Real-data acceptance remains unproven by definition; every supplied workbook is treated only as a test fixture and the real production dataset has not been imported.
- Allowlisted email provider acceptance is proven; inbox arrival, rendering and RSVP click are unproven until the owner confirms them on the recipient device.
- Allowlisted WhatsApp provider acceptance inside an open 24-hour service window is proven for agenda, travel and stay; phone arrival and content inspection remain unproven. Production sending outside that window still requires final Meta-approved templates and exact Acele workflow mappings.
- Core committee review and approval of final English, German and Japanese wording.
- Physical iPhone/Android acceptance of the native keyboard, photo picker, camera capture and real video codec playback. Browser input contracts and synthetic R2 range behavior are proven; the physical device behavior is unproven.
- Custom-domain deployment and pilot acceptance. Public Sites access is active for dummy-data RSVP testing; this does not approve the current employee login model for production launch.
- Public employee login approval. The shared first-time PIN must be replaced with unique claim codes or all claims completed behind a private access gate; login throttling also needs an edge policy and uniform failure responses.
